# Styling Examples

| Example | Keywords | Description |
|---------|----------|-------------|
| [Custom Defaults](custom_defaults.md) | styling, defaults, overrides, set_default_color | Global default styles and per-call overrides |

## Keywords

defaults · overrides · set_default_color · styling
