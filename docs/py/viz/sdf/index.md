# SDF Viewer

Ray-marched signed-distance-function rendering — both as a dedicated fullscreen
viewer and as a per-object opt-in in the standard viewer.

| Guide | What you will learn |
|-------|---------------------|
| [SDF Viewer](sdf-viewer.md) | The dedicated ray-marched `pytanga.viz.sdf` viewer: analytic + algebra paths, distance/opacity functions, boolean combine modes |
| [SDF Objects in the Standard Viewer](sdf-objects.md) | Per-object SDF rendering in the standard viewer: `SdfStyle`, `SdfObject`/operators, `SdfGroup` |
