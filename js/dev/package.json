{
  "name": "tanga-js-dev",
  "version": "1.0.0",
  "private": true,
  "description": "Tanga frontend JS toolchain (tests, syntax check, headless smoke).",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node tests/check-syntax.mjs",
    "smoke": "node tests/reconcile-smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright": "^1.63.0"
  }
}
