# Phase 4 — P3 hard-coded type masks

## Goal

Hard-code `mask_for_<type>(basis)` for every supported type in `BasisP3`.

## Files

- Edit: `py/pytanga/geometry/create_p3.py`
- Edit: `py/tests/geometry/test_geometry_mask.py`

## Steps

- [x] **4.1 — enumerate + determine**
  - List P3-supported types (`Point`, `Direction`, `Line`, `Plane`, `Space`,
    `Rotor`, `ReflectionPlane`, `ReflectionLine`, `ReflectionPoint`) and
    determine each full blade set.

- [x] **4.2 — hard-code**
  - Add `mask_for_<type>(basis) -> BladeMask` for each type with explicit ids.

- [x] **4.3 — tests**
  - Pin `mask_for(alg, Type).ids` and `.basis_names`; pin instance-subset.

## Validation

`uv run pytest py/tests/geometry/test_geometry_mask.py -q`

---

> **Architecture note — check the developer docs.** Before implementing, check
> `docs/dev/` (especially `docs/dev/architecture/`) for the subsystem(s) this
> work touches, so the new code aligns with the documented architecture.  If
> this work introduces or changes architecture, update the developer docs.
