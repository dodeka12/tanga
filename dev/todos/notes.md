problems:

- an ipns sphere is analyzed as sphere even if the analyze function uses opns=True
- need docs on how to use the geo class to fix the OPNS value for all create and analyze calls.

- when opening the browser, add a note that the websocket port also has to be reachable (forwarded) for the viz to work.  Otherwise, the browser will just show a blank page.

- Geometry.create() has a syntax error

- we need to be able to create a Point() from an E3 grade-1 vector.

- add function to get E3 representative vector.

- add some functionality to draw a tail of a specified number of points for an animated object

- add the posibility to draw a 3d path.

- add possibility to draw surface from path over multiple timesteps.

- entites like line segments and plane segments.

 