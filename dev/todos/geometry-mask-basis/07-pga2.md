# Phase 7 — PGA2 named-basis sweep

## Goal

Ensure `mask_for(typ)` carries the correct named basis for every type supported
in `BasisPGA2` (2-D plane-based PGA; Gunn/Dorst display basis).

## Files

- Edit: `py/pytanga/geometry/create_pga2.py`
- Edit: `py/tests/geometry/test_geometry_mask.py`

## Steps

- [x] **7.1 — scan**
  - Enumerate PGA2-supported types and record `mask_for(typ).ids` / `.basis_names`.

- [x] **7.2 — adapt**
  - Add `basis_for_<type>(basis)` to `create_pga2.py` where needed; confirm the
    auto display basis elsewhere.

- [x] **7.3 — tests**
  - Pin `mask_for(typ).basis_names` for every PGA2-supported type.

## Validation

`uv run pytest py/tests/geometry/test_geometry_mask.py -q`

---

> **Architecture note — check the developer docs.** Before implementing, check
> `docs/dev/` (especially `docs/dev/architecture/`) for the subsystem(s) this
> work touches, so the new code aligns with the documented architecture.  If
> this work introduces or changes architecture, update the developer docs.
