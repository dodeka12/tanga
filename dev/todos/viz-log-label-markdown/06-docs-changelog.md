# Phase 6 — Docs, examples, changelog

## Goal

Document the three views, add runnable examples (including live updates and a
KaTeX formula), regenerate the example gallery, and record the change in the
branch changelog.

## Files

- Edit: `docs/py/viz/visualizer/split-views.md`
- New: `py/examples/viz/scenes/display_views.py`
- New: `py/examples/viz/scenes/log_view.py`
- Edit: generated `docs/py/examples/**` (via the example-docs generator)
- Edit: `docs/changelog/2026-09-03_feat-view-architecture.md`

## Steps

- [x] **6.1 — Reference docs (`split-views.md`)**
  - Add `LabelView` / `MarkdownView` / `LogView` rows to the "View Hierarchy"
    table and a `### Display views` section documenting each: `LabelView`
    (`value`, `font_size`), `MarkdownView` (`value`, marked + KaTeX), and
    `LogView` (`log`/`get_log`/`write_file`/`load_file`/`clear`, `max_history`,
    two-column render, `log_update`).

- [x] **6.2 — `display_views.py` example**
  - A vertical `SplitView` with a `LabelView` pane and a `MarkdownView` pane
    (markdown includes a `$…$` formula), plus a control that calls
    `viz.set_control(…)` to change the label and markdown live (demonstrating
    settability).  Update the module docstring + `Keywords:` header per
    `dev/workflows/example-docs.md`.

- [x] **6.3 — `log_view.py` example**
  - A `SplitView` with a `LogView` pane (a `max_history`) and a button whose
    handler calls `log_view.log(...)` with a str and a dict (live push);
    pre-populate a few lines and show `get_log()` / `write_file()` usage.
  - Update the docstring + `Keywords:` header.

- [x] **6.4 — Regenerate example docs**
  - Run `uv run python tools/generate-example-docs.py` and `--check`; revert any
    unrelated generated-doc drift (as in prior example changes).

- [x] **6.5 — Changelog**
  - Run `uv run python tools/last-release.py` for the title (`1.17.0`).
  - Append a `## New Features` bullet to
    `docs/changelog/2026-09-03_feat-view-architecture.md` for `LabelView` /
    `MarkdownView` / `LogView`.

## Validation

```
uv run pytest
uv run mkdocs build --strict
```

## Notes

- `site/` is generated by `mkdocs build`; do not hand-edit it.
- The changelog keeps its branch name until PR time (renamed per
  `dev/workflows/pull-request.md`).
