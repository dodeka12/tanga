# problems:

- (ok) remove .from_name() in the docs for the algebra class.
- (ok) remove ReflectionOrigin from the docs.
- (ok) update docs for GeneralRotor()
- (ok) ensure that ~A is the reverse, not the inverse.

# features:

- imlement operator functions for multivectors that (mostly) fit the galgebra definitions.
- allow picking out all elements of a specific grade from an MV by using round brackets like a(2) to get all grade 2 elements.

- add function to get E3 representative vector.
- add some functionality to draw a tail of a specified number of points for an animated object
- add the posibility to draw a 3d path.
- add possibility to draw surface from path over multiple timesteps.
- entites like line segments and plane segments.

 